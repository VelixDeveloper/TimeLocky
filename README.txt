TimeLocky — Protected Distribution
====================================

1. Install Python 3.11+
2. pip install -r requirements.txt
3. Place dev.env beside run.py:
      API_BASE_URL=http://your-server/backend
      API_SECRET=your-api-secret
4. python run.py

If you see 'Access denied', ask your admin to register
your machine via the Telegram bot.
